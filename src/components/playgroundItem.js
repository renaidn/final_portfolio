import React from "react";
import styles from "../styles/playgroundItem.module.css";
import Carousel2 from "./carousel2"; // Import the Carousel component

const PlaygroundItem = ({ project }) => {

  return (
    <div>
      <im
    </div>
  );
};

export default PlaygroundItem;
